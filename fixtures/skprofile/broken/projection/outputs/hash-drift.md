<!--
Muster-local discrimination-control fixture output artefact (WP05
remediation, F-1 §7.3). This file stands in for a real Spec Kitty
`.claude/agents/<id>.md` projection output. Its real, independently
recomputed sha256 deliberately does NOT match the `file_hash` recorded in
`../../projection-manifest.json`'s entry for "projection-hash-drift" — that
mismatch is the entire point of this fixture (rubric §7.3, warning
severity).
-->
# Projection Hash Drift (fixture output)

This is a stand-in projected native-agent-profile artefact for the
`projection-hash-drift` discrimination-control fixture. Its content is
irrelevant beyond being non-empty and stable — only its sha256 digest
matters, and that digest is deliberately NOT what the projection manifest
records.
