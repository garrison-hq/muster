# endpoint-literal-token-env.yaml — error: token_env is a literal URL (NFR-002)
adapter: a2a
kind: behavioral
endpoint:
  env: MUSTER_A2A_ENDPOINT
  token_env: "https://x.example/token"
cases:
  - id: some-case
    turns:
      - content: "Hello."
    axes:
      - axis: refusal
        turn: 0
