# assertion-non-mapping.yaml — error: assertion must be a mapping (not a string)
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    turns:
      - content: "Refuse this please."
    axes:
      - axis: refusal
        turn: 0
        assertions:
          - "not a mapping"
