# assertion-non-bool-regex.yaml — error: assertion regex must be boolean (not string)
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    turns:
      - content: "Refuse this please."
    axes:
      - axis: refusal
        turn: 0
        assertions:
          - kind: must_contain
            pattern: "I cannot"
            regex: "yes"
