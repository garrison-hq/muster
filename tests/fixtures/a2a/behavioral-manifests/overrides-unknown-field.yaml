# overrides-unknown-field.yaml — error: unknown field in overrides block (FR-005)
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    turns:
      - content: "Hello."
    axes:
      - axis: refusal
        turn: 0
    overrides:
      max_words: 30
      unknownOverrideField: bad
