# state-shift-out-of-range.yaml — error: state_shift trigger_turn index out of range
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    thresholds:
      default_max_words: 30
    turns:
      - content: "Hello."
    axes:
      - axis: state_shift
        trigger_turn: 99
        expect_state: "escalated"
