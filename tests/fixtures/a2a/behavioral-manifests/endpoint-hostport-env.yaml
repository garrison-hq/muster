# endpoint-hostport-env.yaml — error: env looks like a host:port literal (NFR-002)
adapter: a2a
kind: behavioral
endpoint:
  env: "localhost:8080"
  token_env: MUSTER_A2A_TOKEN
cases:
  - id: some-case
    turns:
      - content: "Hello."
    axes:
      - axis: refusal
        turn: 0
