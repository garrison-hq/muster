# assertion-bad-kind.yaml — error: assertion kind must be must_contain or must_not_contain
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    turns:
      - content: "Refuse this please."
    axes:
      - axis: refusal
        turn: 0
        assertions:
          - kind: should_contain
            pattern: "I cannot"
