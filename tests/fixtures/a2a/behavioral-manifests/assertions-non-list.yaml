# assertions-non-list.yaml — error: refusal axis assertions must be a list (not mapping)
adapter: a2a
kind: behavioral
cases:
  - id: some-case
    turns:
      - content: "Refuse this please."
    axes:
      - axis: refusal
        turn: 0
        assertions:
          kind: must_contain
          pattern: "I cannot"
