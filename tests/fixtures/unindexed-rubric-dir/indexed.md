# Indexed Fixture Doc

This fixture document is referenced by name from `index.md` in this same
directory, and has zero code citations of its own — it exists only to prove
the index-completeness check does NOT flag a properly-indexed file, in
contrast with `orphan.md` in this same fixture directory.
