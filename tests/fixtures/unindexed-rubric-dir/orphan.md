# Orphan Fixture Doc

This fixture document is deliberately NOT mentioned anywhere in `index.md`
in this same directory. It has zero code citations of its own, so running
the drift-check against this fixture directory proves the index-completeness
failure mode fires independently of any citation-resolution failure (T008's
own edge-case requirement).
