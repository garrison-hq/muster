# Issue matrix — skills-behavioral-enablement-01KYJFAC

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #62 | Negative-expectation static skill cases pass when their fixture is absent (runStaticSkillCase catch block) | in-mission | Issue #62's own catalogued primary/headline finding is `runStaticSkillCase`'s catch-block bug (FR-007) — assigned to WP02, still `planned` per `status.json`, not yet implemented; this mission has not fixed #62's own reported defect yet. WP01 separately fixed a related-but-distinct discrimination-control honesty bug it found in the *behavioral* path (not part of #62's own report): `isControl` was always reported `false` because the CLI sent the skill's frontmatter name instead of the literal `rigged-impossible-control` tool name `trigger.ts`'s own derivation checks for, so `trigger.ts:472-478`'s model-quality warning was dead code (commit `66906f5`, "honest isControl tool-name wiring", HIGH-1 remediation). Verified in this tree: `pnpm vitest run tests/skills/cli.test.ts -t "HIGH-1 regression"` passes for real, exercising the previously-unreachable `console.warn` path. The a2a `applyControlInversion` instance from #62's own Blast-radius section is explicitly out of scope for this mission (spec.md Scope Guard / Decision), filed separately as `garrison-hq/muster#69`. |
| #59 | [M5] skills-behavioral-enablement — make `muster skills run` execute behavioral trigger cases; fix env + manifest seams | in-mission | Delivered so far: FR-001 (behavioral cases now execute via `runTriggerConformance`) and FR-002 (`MUSTER_ENDPOINT` canonical, `MUSTER_BASE_URL` deprecated alias with warning) shipped in WP01 (commits `c2966d9`, `66906f5`, `38041d9`, `a58fa0a`; lane state `for_review`); C-001/C-004 (errored-run-fails-closed, exit contract) regression-tested in the same WP. FR-004 (rubric doc `docs/rubric/skills-trigger-taxonomy.md` published, OQ-1 resolved, fabricated citation repointed) shipped in WP03 (commit `ed0dbfd` plus three citation-accuracy fixes; lane state `for_review`). Not yet delivered: FR-003 (Ajv manifest schema) and FR-007 (issue #62's catch-block fix) — both assigned to WP02, `planned`/not started; FR-005 (formal CLI-reachability verification of the discrimination-control flow) and FR-006 (`examples/skills/manifest.yaml` behavioral + control case, mock-client tests) — both assigned to WP04, `planned`/not started. Per `status.json` at time of writing, WP02 and WP04 have not begun; this mission is not yet `done`. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
