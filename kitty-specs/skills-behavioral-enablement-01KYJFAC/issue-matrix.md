# Issue matrix — skills-behavioral-enablement-01KYJFAC

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #62 | Negative-expectation static skill cases pass when their fixture is absent (runStaticSkillCase catch block) | fixed | Issue #62's primary, headline finding — `runStaticSkillCase`'s (`src/cli/index.ts`) catch-block fail-open bug — is fixed by WP02/T010 (commit `5d9170d53`, "fix(WP02): T010 runStaticSkillCase catch-block fail-closed fix (FR-007, muster#62)", lane-a). Verified this session (2026-07-28) two ways: (a) `pnpm vitest run tests/skills/cli.test.ts -t "delete-direction"` passes, exit 0, in the lane-a worktree; (b) manual reproduction against the built CLI on an isolated local merge of lane-a+lane-b (`/home/jeroennouws/scratch-m5-verify`): a case with `expectations.ok:false` against a temp-copied `fixtures/skills/broken/name-dir-mismatch` reports `passed:true`, exit 0 before deletion; after `rm -rf` of the copy, the same manifest reports `{"passed":false,"errored":true,...}`, exit 1 — the delete-direction fix, not merely the flip-direction case. WP01 separately fixed a related-but-distinct discrimination-control honesty bug in the *behavioral* path (not part of #62's own report): `isControl` was always reported `false` because the CLI sent the skill's frontmatter name instead of the literal `rigged-impossible-control` tool name `trigger.ts`'s own derivation checks for (commit `66906f5`, "honest isControl tool-name wiring", HIGH-1 remediation) — verified: `pnpm vitest run tests/skills/cli.test.ts -t "HIGH-1 regression"` passes for real. The a2a `applyControlInversion` instance from #62's own Blast-radius section remains explicitly out of scope for this mission (spec.md Scope Guard / Decision), filed separately as `garrison-hq/muster#69` — not part of this verdict. |
| #59 | [M5] skills-behavioral-enablement — make `muster skills run` execute behavioral trigger cases; fix env + manifest seams | fixed | All seven FRs #59 named are now delivered and independently re-verified this session (2026-07-28) on both lane-a alone and an isolated local merge of lane-a+lane-b (zero-conflict merge, `/home/jeroennouws/scratch-m5-verify`): FR-001/FR-002 (behavioral cases execute; `MUSTER_ENDPOINT` canonical / `MUSTER_BASE_URL` deprecated-alias) — WP01, tests AC-1a/AC-2a/AC-2b/AC-2c pass exit 0; FR-003 (Ajv manifest schema, exit 2) — WP02, 3/3 tests pass plus manual exit-2 reproduction; FR-004 (rubric doc + fabricated-citation repoint) — split WP01 (trigger.ts citation) / WP03 (doc + remaining sites), fabricated-anchor grep count is 0 only on the merged tree (3 residual hits in lane-a alone, 1 in lane-b alone — each lane fixes only the files it owns); FR-005 (control CLI-reachable) — live-model run recorded in `quickstart.md`, `passed:false` on 3 independent runs; FR-006 (`examples/skills/manifest.yaml` behavioral+control mock-client cases) — WP04, test passes exit 0; FR-007/#62 (catch-block fail-closed) — WP02, see #62's own row above. C-001/C-002/C-003/C-004 also independently re-verified this session (errored-run regression test, byte-identical `gradeAxis`, green `invariants.test.ts` on a clean `.env`-free clone, and a manual 0/1/2 exit-code battery against the built CLI). Full offline suite: 170 files / 3637 passed / 3 skipped, exit 0, reproduced on the merged tree. Not independently re-executed this session: the live-model gate itself (cited from `quickstart.md`'s filled Results table and WP04's approval note, not re-run live here) and the SonarCloud quality gate (mission not yet pushed/PR'd). See `acceptance-matrix.json` for the full per-criterion breakdown. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
