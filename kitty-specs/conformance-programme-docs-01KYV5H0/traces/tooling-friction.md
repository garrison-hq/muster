# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-07-31 · claude (WP02 implementer) · C-001's verification command is broken in a second, distinct way beyond WP01's finding. Even WP02's own task-file-corrected pattern (adding scripts/check-(rubric-citations|register-schema).mjs and tests/ to the exclusion set) still flags kitty-ops/**.jsonl and kitty-specs/conformance-programme-docs-01KYV5H0/** (spec-kitty's own event-log/planning bookkeeping, which changes on every mark-status/acceptance-verdict call) as false violations, on top of WP01's already-recorded examples/README.md and .env.example false positives. Additionally, <base> must NOT be resolved via 'git merge-base main HEAD': this feature branch (single_branch topology) diverged from main at 8953ee844 (2026-07-09, before the skills-behavioral-enablement and spec-kitty-profile-adapter missions merged onto this same lineage), so that merge-base drags in ~50 unrelated commits as false diff content. The correct <base> is this mission's own fork point, e042d7198c575f4fc2a815cfa7b201aff6948b30 (independently confirmed identical to spec.md's own pinned 16f0d34c3 reference commit, matching the mission's first 'Add meta' commit's parent). Corrected, fully-discriminating pattern used for this WP's own C-001 evidence: git diff --name-only $BASE...HEAD | command grep -Ev '^(docs/|site/|scripts/check-(rubric-citations|register-schema)\.mjs|tests/|kitty-ops/|kitty-specs/|\.env\.example$|examples/README\.md$)' — verified clean (exit 1, no output) against the real committed diff, and verified to actually fire (exit 0, naming the file) against a staged-then-reverted scratch touch to src/adapters/skills/schema.ts.

2026-07-31 · claude (WP02 implementer) · T008/T016's stated pre-repair expectation ('exit 1, naming all eight anchors') is not achievable by a strictly-conforming canonical-grammar-only parser, and is not what a real, non-cheating implementation produces. Flattening newline-wrapping and regex-scanning the pre-repair skills-trigger-taxonomy.md shows only 4 citations actually match the canonical `symbol` (`file:line`) two-backtick-group grammar: gradeAxis (x2, both wrong), console.warn (x1, wrong), QueryRunResult (x2, both already correct). The other 5 of the 8 repair-inventory items (return-unmutated, the static-mode block, its assertion, the live-model block, its assertion, and the SC-004-label sentence) are, in their real pre-repair text, either a bare single-backtick file:line with no leading symbol+parens, or a fully path-less '(line N)' reference — both are, by FR-002's own documented design, invisible to a parser that only understands the canonical grammar (the same category as spec-kitty-behavioral-axes.md's one acknowledged blind-spot citation). Real, captured Commit-A output (independently re-verified against the actual committed tree, not just the working copy) names only 3 stale anchors (2x gradeAxis, 1x console.warn), not 8, and exits 1 for that reason (plus, incidentally, because docs/rubric/index.md does not exist yet at that commit, which independently triggers this WP's index-completeness check on all 6 files — a second, distinct reason for the same exit code). The repair itself (Commit B) still performs all 8 corrections plus the grammar normalization exactly as specified; only the pre-repair *visibility* claim in T008/T016's prose is overstated.
